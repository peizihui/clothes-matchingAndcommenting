<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.clothes.dao.UserDAO">
    <insert id="insertUser">
        INSERT INTO USER (permission, user_name, password, icon, email)
        VALUES (#{permission}, #{userName}, #{password}, #{icon}, #{email})
    </insert>
    <delete id="deleteUser">
        DELETE FROM USER WHERE user_name = #{userName}
    </delete>
    <update id="updateUser">
        UPDATE USER SET
          password = #{password}
          WHERE user_name = #{userName}
    </update>
    <select id="queryUserByUserName" resultType="User">
        SELECT *
        FROM USER
        WHERE user_name = #{userName}
    </select>
    <select id="queryAll" resultType="User">
        SELECT *
        FROM USER
        limit #{offset}, #{limit}
    </select>
    <select id="queryByUserNameAndPassword" resultType="User">
        SELECT *
        FROM USER
        WHERE user_name = #{userName} AND password = #{password}
    </select>
</mapper>